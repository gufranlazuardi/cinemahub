import LoginForm from "@/components/LoginForm";

const Login = () => {
  return (
    <div className="flex flex-col w-full h-screen pt-[15rem] items-center">
      <LoginForm />
    </div>
  );
};

export default Login;
